import Vue from 'vue'
// 按需引入vant组件
import {
    NavBar,
    Icon,
    Search,
    Toast,
    Collapse,
    CollapseItem
} from 'vant'

Vue.use(NavBar)
    .use(Icon)
    .use(Search)
    .use(Toast)
    .use(Collapse)
    .use(CollapseItem)